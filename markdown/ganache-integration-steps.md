# 🚀 ขั้นตอนการตั้งค่า Ganache เพื่อรัน Demo ระบบ Vehicle NFT

การนำ Smart Contract (ไฟล์ .sol) ไปรันบน Ganache (Local Blockchain) และเชื่อมต่อเข้ากับระบบ Frontend (React/Vite) และ Backend (NestJS) ของคุณ มีขั้นตอนที่ต้องทำตามลำดับดังนี้:

## 🌟 1. ตั้งค่า Smart Contract Development Environment (Hardhat)
โปรเจกต์ของคุณมีแต่ไฟล์ `.sol` ในโฟลเดอร์ `contracts/` ภายนอกสุด แต่ยังไม่มีเครื่องมือสำหรับ Compile และ Deploy
- [x] เปิด Terminal และสร้างโฟลเดอร์สำหรับจัดการเรื่อง Blockchain โดยเฉพาะ เช่น `mkdir smart-contracts`
- [x] เข้าไปในโฟลเดอร์นั้น `cd smart-contracts` และรัน `npm init -y`
- [x] ติดตั้ง Hardhat ด้วยคำสั่ง: `npm install --save-dev hardhat`
- [x] รันคำสั่งตั้งค่าเริ่มต้นโปรเจกต์: `npx hardhat init` (แนะนำให้เลือก Create a TypeScript หรือ JavaScript project)
- [x] คัดลอก/ย้าย ไฟล์ `.sol` ทั้งหมดของคุณจากโฟลเดอร์ `contracts/` เดิม เอาไปไว้ภายในโฟลเดอร์ `contracts/` ของโปรเจกต์ Hardhat ที่เพิ่งสร้าง

## 🛠️ 2. ตั้งค่า Ganache & Hardhat Config
- [ ] ดาวน์โหลดและเปิดโปรแกรม **Ganache UI** (หรือหากถนัด CLI ให้รัน `ganache-cli`)
- [ ] สร้าง Workspace ใหม่แบบ Quickstart รัน Local Blockchain ขึ้นมา
- [x] จดจำค่า **RPC Server** ด้านบนของหน้าจอ Ganache (ส่วนใหญ่มักจะเป็น `http://127.0.0.1:7545` หรือ `http://127.0.0.1:8545`)
- [x] ย้อนกลับมาดูไฟล์ `hardhat.config.js` (หรือ `.ts`) ในโปรเจกต์ Hardhat ของเรา
- [x] เพิ่มการตั้งค่า Network Ganache ลงไปโดยนำ Private Key สัก 1-2 บัญชีจาก Ganache มาใส่ด้วย (เผื่อเป็นบัญชีคนทำ Deploy):
  ```javascript
  module.exports = {
    solidity: "0.8.20", // ให้ตรงกับเวอร์ชันที่ระบบกำหนดไว้บนสุดในไฟล์ .sol
    networks: {
      ganache: {
        url: "http://127.0.0.1:7545", // รันพอร์ตไหนให้แก้ให้ตรงกัน
        accounts: ["0xf42bb2eb2148588fdce415a8b1891e3b6913449422fed9b9373edb9e9793834e"] // เอา Private Key จาก Ganache มาวาง
      }
    }
  };
  ```

## 🏗️ 3. เขียน Script สำหรับ Deploy Contract
- [x] สร้างไฟล์ Deployment Script ในโฟลเดอร์ `scripts/` (เช่นสร้างชื่อว่า `deploy.js`)
- [x] เขียนโค้ดใน Script ให้ Deploy contracts ที่สำคัญเรียงตามลำดับความเกี่ยวข้องกัน (ตัวอย่าง):
  - `VehicleRegistry.sol`
  - `VehicleNFT.sol`
  - `VehicleLifecycle.sol`
  - `VehicleLien.sol`
  - `VehicleConsent.sol`
- [ ] รันคำสั่งเพื่อคอมไพล์โค้ด: `npx hardhat compile`
- [ ] รันคำสั่งทำการ Deploy ไปที่ Ganache: `npx hardhat run scripts/deploy.js --network ganache`
- [ ] **ข้อแนะนำสำคัญ:** หลังจาก Deploy เสร็จบนคอนโซลจะสรุป **Contract Address** ออกมา ให้บันทึก/ก็อปปี้ Address เหล่านี้ไว้ทั้งหมด จำเป็นต้องเอาไปให้ Frontend และ Backend เรียกใช้

## 🌐 4. ตั้งค่าฝั่ง Frontend (vehicle-nft-prototype)
- [ ] ไปที่ผลลัพธ์ของการคอมไพล์ใน Hardhat (โฟลเดอร์ `artifacts/contracts/`) คุณจะเจอไฟล์ย่อยเป็น `.json` ซึ่งคือ **ABI**
- [ ] นำไฟล์ ABI (.json) ทั้งหมดที่ต้องใช้ไปวางในโค้ดฝั่ง Frontend (เช่นสร้างโฟลเดอร์ `vehicle-nft-prototype/src/abi/`)
- [ ] สร้างไฟล์ใหม่เพื่อเก็บ Contract Address (เช่น `src/config/contracts.ts`) เอา Address ที่ก็อปปี้ไว้จากข้อ 3 มาใส่ให้ตรงกับตัวแปรต่างๆ
- [ ] หาระบบเชื่อมต่อ Web3 ของคุณ (ปัจจุบันคุณลง library `ethers` ไว้แล้ว) ตอนที่สั่ง new Provider ให้ชี้ไปหาค่ายิ่ง Ganache URL:
  ```javascript
  const provider = new ethers.JsonRpcProvider("http://127.0.0.1:7545");
  ```
- [ ] **(เพิ่มเติมการใช้ทดสอบจริง)** เปิด Google Chrome -> Metamask -> เพิ่มเครือข่ายใหม่ ระบุเป็น Localhost (URL เป็น `http://127.0.0.1:7545` และ Chain ID ปกติของ Ganache คือ `1337` หรือ `5777`) -> จากนั้น Import Account ใหม่ด้วย Private Key ตัวอื่นๆ ในวง Ganache เพื่อให้มีเหรียญ ETH ไว้ทำธุรกรรมจำลองบนหน้าเว็บ

## ⚙️ 5. ตั้งค่าฝั่ง Backend (NestJS)
ตอนนี้ Backend ของคุณในโฟลเดอร์ `backend` เป็น NestJS + MySQL ที่ยังไม่ได้คุยกับ Blockchain เลย (ผมเช็กจาก package.json แล้ว) แต่ระบบ Vehicle ของคุณ Backend จะมีบทบาทเป็น Admin ด้วย แน่นอนว่าต้องมีฝั่งนี้:
- [ ] ผ่าน Terminal ให้คุณ `cd backend` และรันสั่งติดตั้ง Ethers.js ก่อนด้วย `npm install ethers`
- [ ] นำโฟลเดอร์ **ABI (.json)** ชุดเดียวกับที่ให้ Frontend ไปวางไว้ที่ Backend ในจุดใดจุดหนึ่ง เช่น `backend/src/blockchain/abi/`
- [ ] เพิ่ม Address และ Provider ของคุณลงในไฟล์ `backend/.env`
  ```env
  GANACHE_RPC_URL=http://127.0.0.1:7545
  ADMIN_PRIVATE_KEY=<GANACHE_PRIVATE_KEY>
  VEHICLE_REGISTRY_ADDRESS=0x...
  VEHICLE_NFT_ADDRESS=0x...
  ```
- [ ] สร้าง Service ทาง NestJS เพื่ออ่านค่า ABI/Address และสร้าง Backend Wallet ขึ้นมา (เชื่อมด้วย `ADMIN_PRIVATE_KEY` ข้างบน)
- [ ] อัปเดต Controller เช่น `backend/src/vehicle/vehicle.controller.ts` ที่คุณเปิดอยู่ ให้เมื่อมีการร้องขอจากหน้าเว็บ ส่งคำสั่งต่อไปยัง Blockchain ผ่าน Service Ethers ที่เพิ่งสร้างใหม่นี้